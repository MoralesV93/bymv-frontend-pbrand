name: Release

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  create_tag_and_dispatch:
    if: >
      github.event.pull_request.merged == true &&
      startsWith(github.event.pull_request.head.ref, 'release/') &&
      github.event.pull_request.base.ref == 'feature/CICD_AutomateRelease'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: feature/CICD_AutomateRelease
          fetch-depth: 0

      - name: Set git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determine tag name
        id: tag
        run: |
          BRANCH="${{ github.event.pull_request.head.ref }}"
          TAG="${BRANCH#release/}"
          if [ "$TAG" = "$BRANCH" ]; then
            echo "Branch name does not start with release/"
            exit 1
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Create annotated tag and push
<<<<<<< HEAD
=======
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
>>>>>>> 275a437 (feat (CICD): created new release workflow)
        run: |
          TAG=${{ steps.tag.outputs.tag }}
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "refs/tags/$TAG"
